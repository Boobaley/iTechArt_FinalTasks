import { makeStyles } from '@material-ui/core/styles';

const useStyles = makeStyles({
    root: {
      flexGrow: 1,
    },
    tabText: {
        color: 'white',
        fontSize: 15
    },
    tab: {
        minHeight:64
    }
  });

export default useStyles;