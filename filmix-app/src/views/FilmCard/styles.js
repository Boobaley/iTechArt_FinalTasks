import { makeStyles } from '@material-ui/core/styles';

export const useStyles = makeStyles({
    card: {
      maxWidth: 345,
      width: 250,
      position: 'absolute',
      zIndex: 1
    },
    media: {
      height: 180,
    },
  });