import { makeStyles } from '@material-ui/core/styles';

export const useStyles = makeStyles(() => ({
    wrapper: {
        margin: '0 auto',
        width: 'max-content'
    },
    label: {
        color: '#fff'
    },
    radioButton: {
        marginRight: 50
    }
  }));